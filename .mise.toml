[tools]
rust = "1.96" # matches Cargo.toml rust-version
node = "latest" # npm-based checks and the checkout's OpenCode plugin
"npm:@opencode-ai/cli" = { version = "next", allow_builds = ["@opencode-ai/cli"] } # OpenCode 2 beta host
just = "latest" # local task runner (`just check`)
"cargo:mdbook" = "0.5.4" # user/contributor documentation
